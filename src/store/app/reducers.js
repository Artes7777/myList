const intialState = {
  tasks : [],
  selected : new Set(),
  isSelectedAll: false,
  date: new Date(),
  todayOrCalendar : null,
};

export const tasksReducer = (state = intialState, action) => {
  
  return state;
}
