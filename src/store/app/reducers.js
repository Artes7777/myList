const intialState = {
  tasks : [],
  filter : 'all',
  selected : new Set(),
  isSelectedAll: false,
  date: new Date(),
  todayOrCalendar : null,
};

export const appReducer = (state = intialState, action) => {
  switch (action.type) {
    case :
  }
  return state;
}
